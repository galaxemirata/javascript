// logical operators
// && (AND)
// || (OR)
// !(NOT)

// Arithmetic Operators e.g + * / - % **
// Assignment operators e.g = += -= *= /= %= **=

let age=20
if (age>=18){
    console.log("You can vote")
}